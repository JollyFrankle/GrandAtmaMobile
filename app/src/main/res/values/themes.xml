<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GrandAtmaHotel" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GrandAtmaHotel" parent="Base.Theme.GrandAtmaHotel">
        <item name="fontFamily">@font/gabarito</item>
        <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="GAH.LoadingDialog" parent="Theme.Material3.Light.Dialog">
        <item name="windowMinWidthMajor">0%</item>
        <item name="windowMinWidthMinor">0%</item>
        <item name="backgroundColor">@color/white</item>
    </style>
</resources>